accessing a character in a string: MyString[0];

string str1 = "Test";
for (int ctr = 0; ctr <= str1.Length - 1; ctr++ )
   Console.Write("{0} ", str1[ctr]);
// The example displays the following output: 
//      T e s t   


string.Format("${0:0.00}", 5.05);   // $5.05
string.Format("${0:0}.", 5.05);     // $5.
string.Format("{0:c}", 5.05);       // $5.05